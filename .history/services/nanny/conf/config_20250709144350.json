{
    "interval":1000,
    "security": {
    "key": "${SERVER_KEY}",
    "cert": "${SERVER_CERT}",
    "chain": "${SERVER_PEM}",
    "cn": "pin"
  },
   "auth": {
    "address": "${AUTH_RPC_HOST}:${AUTH_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  }
}