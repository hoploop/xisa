{
  "address": "${PLAYER_RPC_HOST}:${PLAYER_RPC_PORT}",
  "detectors":"../detector/data/detectors",
  "security": {
    "key": "${SERVER_KEY}",
    "cert": "${SERVER_CERT}",
    "chain": "${SERVER_PEM}",
    "cn": "pin"
  },
  "database": {
    "uri": "${DATABASE_URI}",
    "name": "pin",
    "filesystem": "fs"
  },
  "api": {
    "address": "${API_RPC_HOST}:${API_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "language": {
    "address": "${LANGUAGE_RPC_HOST}:${LANGUAGE_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  }
  
}
