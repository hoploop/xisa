import { DetectorTrainCountPipePipe } from './detector-train-count-pipe.pipe';

describe('DetectorTrainCountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DetectorTrainCountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
