import { RecordFramePipe } from './record-frame.pipe';

describe('RecordFramePipe', () => {
  it('create an instance', () => {
    const pipe = new RecordFramePipe();
    expect(pipe).toBeTruthy();
  });
});
