{
  "player": {
    "address": "${PLAYER_RPC_HOST}:${PLAYER_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "database": {
    "uri": "${DATABASE_URI}",
    "name": "pin",
   "filesystem":"fs"
  },
  "workspace": {
    "address": "${WORKSPACE_RPC_HOST}:${WORKSPACE_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },

  "detector": {
    "address": "${DETECTOR_RPC_HOST}:${DETECTOR_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "auth": {
    "address": "${AUTH_RPC_HOST}:${AUTH_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "recorder": {
    "address": "${RECORDER_RPC_HOST}:${RECORDER_RPC_PORT}",
    "key": "${CLIENT_KEY}",
    "cert": "${CLIENT_CERT}",
    "chain": "pin",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "translations": "${TRANSLATIONS_PATH}",
  "service": {
    "address": "${API_RPC_HOST}:${API_RPC_PORT}",
    "security": {
      "key": "${SERVER_KEY}",
      "cert": "${SERVER_CERT}",
      "chain": "${SERVER_PEM}",
      "cn": "pin"
    }
  },
  "api": {
    "version": "3.0.0",
    "title": "API",
    "description": "Personal Intelligent Network",
    "summary": "Your AI Assistant",
    "swagger_persistent": true,
    "openapi_url": "/openapi.yaml",
    "cors": {
      "origins": ["*"],
      "allow_credentials": true,
      "allow_methods": ["*"],
      "allow_headers": ["*"]
    },
    "tags": [
      { "name": "ws", "description": "WebSocket management" },
      { "name": "auth", "description": "Auth management" },
      { "name": "detector", "description": "Detector management" },
      { "name": "recorder", "description": "Record management" },
      { "name": "project", "description": "Project management" },
      { "name": "ui", "description": "UI management" },
      { "name": "player", "description": "Player management" },
      { "name": "core", "description": "Core management" },
      { "name": "audio", "description": "Audio management" },
      { "name": "utils", "description": "Utils management" },
      { "name": "store", "description": "Store management" },
      { "name": "prompt", "description": "Prompt management" },
      { "name": "language", "description": "Language management" },
      { "name": "vision", "description": "Vision management" },
      { "name": "theme", "description": "Theme management" }
    ]
  }
}
